# 智能体代码规划：从故事到实现

## 🎯 开篇：一个智能体的自述

大家好，我是一个智能代码生成体。今天我想用讲故事的方式，带你走进我的世界，了解我是如何从一个简单的任务描述，一步步生成出完整、可执行的代码的。

---

## 第一章：接到任务

### 故事开始

有一天，我的主人给了我一个任务：

> "读取一个CSV文件，清洗数据去掉空值，然后计算统计信息，最后把结果保存起来。"

听起来很简单对吧？但对我来说，这句话背后隐藏着许多需要解决的问题：
- 📝 具体要做哪些步骤？
- 🔗 这些步骤之间有什么关系？
- 💡 需要哪些变量？
- 🎯 这些变量在哪里定义、在哪里使用？
- ⚙️ 怎么保证代码运行时不会出现"变量未定义"的错误？

让我带你看看我是如何思考和解决这些问题的。

---

## 第二章：理解任务 - 我的思考过程

### 2.1 第一步：倾听与理解

当我收到任务描述时，我首先要做的是**理解**。就像你听别人说话时要听懂意思一样，我需要从文字中提取关键信息。

让我分析一下这个任务：

```
"读取一个CSV文件，清洗数据去掉空值，然后计算统计信息，最后把结果保存起来。"
```

我会把这句话拆解成几个关键动作：
1. 📥 **读取** CSV文件
2. 🧹 **清洗** 数据（去掉空值）
3. 📊 **计算** 统计信息
4. 💾 **保存** 结果

### 2.2 发现任务之间的故事线

这几个动作不是孤立的，它们之间有着紧密的联系，就像故事的情节一样，有先后顺序：

```
开始 → 读取文件 → 清洗数据 → 计算统计 → 保存结果 → 结束
```

用生活中的例子来说：
- 你得先**买菜**（读取数据）
- 然后**洗菜**（清洗数据）
- 接着**做菜**（计算统计）
- 最后**装盘**（保存结果）

你不可能先做菜再买菜，对吧？这就是**依赖关系**。

### 2.3 我看到的数据流动

在这个过程中，数据像接力赛一样在各个步骤间传递：

```
CSV文件 → [读取] → 原始数据df
              ↓
         [清洗] → 干净数据df_cleaned
              ↓
         [计算] → 统计结果statistics
              ↓
         [保存] → 输出文件
```

每个步骤都需要上一步的**输出**作为**输入**。这就像工厂的流水线，每个工位都要接收前一个工位的成果。

---

## 第三章：制定计划 - 我的规划策略

### 3.1 把大任务变成小任务

我决定把这个大任务分解成4个小模块：

**模块1：数据加载模块**
```
职责：读取CSV文件
需要：文件路径（input_file）
产出：数据表（df）
```

**模块2：数据清洗模块**
```
职责：去除空值
需要：原始数据（df）
产出：清洗后的数据（df_cleaned）
```

**模块3：统计分析模块**
```
职责：计算统计信息
需要：清洗后的数据（df_cleaned）
产出：统计结果（statistics）
```

**模块4：结果保存模块**
```
职责：保存到文件
需要：统计结果（statistics）和输出路径（output_file）
产出：保存的文件
```

### 3.2 画出依赖地图

现在我知道了每个模块的"需要"和"产出"，我可以画出它们之间的依赖关系：

```
        input_file (需要从外部获取)
              ↓
        ┌──────────┐
        │  模块1   │ 读取CSV
        │ (加载)   │
        └────┬─────┘
             │ df
             ↓
        ┌──────────┐
        │  模块2   │ 去除空值
        │ (清洗)   │
        └────┬─────┘
             │ df_cleaned
             ↓
        ┌──────────┐
        │  模块3   │ 计算统计
        │ (分析)   │
        └────┬─────┘
             │ statistics
             ↓
        ┌──────────┐
        │  模块4   │ 保存结果
        │ (保存)   │
        └──────────┘
             ↓
        output_file (需要从外部获取)
```

### 3.3 发现问题：缺失的拼图

看着这个依赖地图，我发现了一个问题：

- 模块1需要 `input_file`，但是这个变量**还没有定义**！
- 模块4需要 `output_file`，但是这个变量也**还没有定义**！

如果我直接生成代码，程序运行时就会报错：`NameError: name 'input_file' is not defined`

这就像你准备做菜时发现没买菜刀一样 - 得先解决缺失的问题！

---

## 第四章：解决难题 - 聪明的变量管理

### 4.1 我的变量追踪系统

为了避免"变量未定义"的错误，我创建了一个**变量注册表**（就像一个账本），记录所有变量的情况：

| 变量名 | 在哪里定义 | 在哪里使用 | 状态 |
|--------|-----------|-----------|------|
| input_file | ？ | 模块1 | ❌ 未定义 |
| df | 模块1 | 模块2 | ✅ 已定义 |
| df_cleaned | 模块2 | 模块3 | ✅ 已定义 |
| statistics | 模块3 | 模块4 | ✅ 已定义 |
| output_file | ？ | 模块4 | ❌ 未定义 |

### 4.2 自动修复：给缺失的变量找个家

当我发现 `input_file` 和 `output_file` 还没有定义时，我会自动生成**初始化代码**：

```python
# 我生成的初始化代码
input_file = ""  # TODO: 请指定输入文件路径
output_file = ""  # TODO: 请指定输出文件路径
```

这就像你发现缺了菜刀，会先去买一把回来。虽然现在还是空的（需要用户填写），但至少变量已经**存在**了，不会报错。

### 4.3 聪明的类型推断

我怎么知道 `input_file` 应该初始化为空字符串 `""`，而不是 `None` 或者 `[]` 呢？

这是因为我有一套**命名规则识别系统**：

```python
# 我的推断规则（启发式）
变量名以 _file 或 _path 结尾  → 推断为字符串类型
变量名以 _list 结尾           → 推断为列表类型
变量名以 _dict 结尾           → 推断为字典类型
变量名以 is_ 或 has_ 开头     → 推断为布尔类型
变量名以 num_ 或 count_ 开头  → 推断为整数类型
```

就像你看到一个人的名字叫"张律师"，你会猜测他是律师一样，我根据变量名的特征来推测它的类型。

---

## 第五章：排好顺序 - 拓扑排序的魔法

### 5.1 问题：如何安排执行顺序？

现在我有了5个模块（包括2个初始化模块），我需要决定它们的执行顺序。

错误的顺序可能是：
```
❌ 先清洗数据 → 再读取文件  （错误！数据还没读取就开始清洗）
❌ 先保存结果 → 再计算统计  （错误！结果还没计算就开始保存）
```

正确的顺序应该是：
```
✅ 初始化变量 → 读取 → 清洗 → 计算 → 保存
```

### 5.2 我的解决方案：拓扑排序

我使用一种叫做**拓扑排序**的算法来自动确定正确的顺序。这个算法的思想很简单：

**规则1**：如果模块B需要模块A的输出，那么A必须在B之前执行。

**规则2**：没有依赖关系的模块可以任意顺序（或并行）执行。

让我用一个简单的例子说明这个过程：

#### 第1步：画出依赖箭头

```
初始化input_file  →  读取模块  →  清洗模块  →  统计模块
初始化output_file  ────────────────────────────────→  保存模块
```

#### 第2步：找出没有依赖的模块（可以最先执行）

```
两个初始化模块没有依赖，可以最先执行
```

####第3步：依次移除已执行的模块，找下一个可以执行的

```
移除初始化模块后：
  读取模块现在没有依赖了，可以执行

移除读取模块后：
  清洗模块现在没有依赖了，可以执行

移除清洗模块后：
  统计模块现在没有依赖了，可以执行

移除统计模块后：
  保存模块现在没有依赖了，可以执行
```

#### 最终顺序

```
1. 初始化 input_file
2. 初始化 output_file  （可以和步骤1并行）
3. 读取模块
4. 清洗模块
5. 统计模块
6. 保存模块
```

这就像排队时，你得先让前面的人办完事，才轮到你。

---

## 第六章：组装代码 - 最后的润色

### 6.1 把所有模块串起来

现在我有了正确顺序的所有模块，该把它们组装成完整的代码了：

```python
# 我组装出的完整代码

"""
智能体自动生成的代码
任务：读取CSV，清洗数据，计算统计，保存结果
"""

import pandas as pd

def main():
    # 步骤1: 初始化输入文件路径
    input_file = ""  # TODO: 请指定输入文件路径
    
    # 步骤2: 初始化输出文件路径
    output_file = ""  # TODO: 请指定输出文件路径
    
    # 步骤3: 读取CSV文件
    df = pd.read_csv(input_file)
    print(f"✓ 成功加载 {len(df)} 行数据")
    
    # 步骤4: 清洗数据
    df_cleaned = df.dropna()
    print(f"✓ 清洗后剩余 {len(df_cleaned)} 行")
    
    # 步骤5: 计算统计信息
    statistics = df_cleaned.describe()
    print("✓ 统计信息：")
    print(statistics)
    
    # 步骤6: 保存结果
    statistics.to_csv(output_file)
    print(f"✓ 结果已保存到 {output_file}")

if __name__ == "__main__":
    main()
```

### 6.2 我的润色工作

生成代码后，我还做了一些"润色"工作：

1. **添加注释**：让代码更容易理解
2. **添加打印语句**：让执行过程可见
3. **添加文档字符串**：说明代码的目的
4. **格式化代码**：让代码整洁美观

就像写完作文要检查错别字和标点符号一样。

---

## 第七章：验证检查 - 确保万无一失

### 7.1 我的自检清单

在交付代码之前，我会进行严格的自我检查：

✅ **语法检查**：代码能不能被Python解释器正确解析？
```python
import ast
ast.parse(code)  # 如果有语法错误会抛出异常
```

✅ **变量检查**：所有使用的变量都定义了吗？
```
检查注册表，确保每个"被使用"的变量都有"定义位置"
```

✅ **导入检查**：需要的库都导入了吗？
```
代码使用了 pd.read_csv() → 需要导入 pandas
```

✅ **逻辑检查**：执行顺序合理吗？
```
确保没有"先用后定义"的情况
```

### 7.2 如果发现问题

如果检查发现问题，我会：

1. **报告问题**：清楚地说明哪里有问题
2. **尝试修复**：如果能自动修复就修复
3. **标记警告**：如果无法确定就用 TODO 标记

就像考试前检查试卷，发现错误及时改正。

---

## 第八章：背后的技术 - 工程实现揭秘

到这里，你已经了解了我工作的完整流程。现在让我们揭开技术的面纱，看看我是如何实现这些功能的。

### 8.1 核心技术1：抽象语法树（AST）

**故事化解释**：
想象你要分析一句话的语法结构，你会把它分解成主语、谓语、宾语。AST就是这样一个工具，但它分析的是代码。

**技术实现**：
```python
import ast

code = "result = x * 2"
tree = ast.parse(code)

# 遍历语法树
for node in ast.walk(tree):
    if isinstance(node, ast.Name):
        if isinstance(node.ctx, ast.Store):
            print(f"定义变量: {node.id}")  # result
        elif isinstance(node.ctx, ast.Load):
            print(f"使用变量: {node.id}")  # x
```

**为什么需要AST**：
- 它能准确识别变量是被**定义**还是被**使用**
- 它能理解代码的结构，不会被注释或字符串中的变量名干扰
- 它是Python内置的，不需要自己写复杂的正则表达式

### 8.2 核心技术2：拓扑排序算法（Kahn算法）

**故事化解释**：
想象你在排队，但有些人之间有"必须排在某人后面"的规则。拓扑排序就是帮你找出一个满足所有规则的排队顺序。

**技术实现**：
```python
def topological_sort(modules):
    # 计算每个模块的"入度"（有多少个依赖）
    in_degree = {m: 0 for m in modules}
    for module in modules:
        for dependency in module.dependencies:
            in_degree[dependency] += 1
    
    # 找出没有依赖的模块（入度为0）
    queue = [m for m in modules if in_degree[m] == 0]
    result = []
    
    # 逐个处理
    while queue:
        # 取出一个没有依赖的模块
        current = queue.pop(0)
        result.append(current)
        
        # 移除这个模块后，更新其他模块的入度
        for next_module in modules:
            if current in next_module.dependencies:
                in_degree[next_module] -= 1
                if in_degree[next_module] == 0:
                    queue.append(next_module)
    
    return result
```

**为什么选择Kahn算法**：
- 简单易懂，容易实现
- 时间复杂度 O(V+E)，效率高
- 能检测循环依赖（如果最后result长度不等于modules数量）

### 8.3 核心技术3：启发式类型推断

**故事化解释**：
就像中医的"望闻问切"，通过观察症状（变量名）来推断病因（类型）。

**技术实现**：
```python
def infer_type(var_name):
    """根据变量名推断类型"""
    rules = {
        # 后缀规则
        '_list': 'list',
        '_dict': 'dict',
        '_df': 'DataFrame',
        '_file': 'str',
        '_path': 'str',
        
        # 前缀规则
        'is_': 'bool',
        'has_': 'bool',
        'num_': 'int',
        'count_': 'int',
    }
    
    # 检查后缀
    for suffix, type_name in rules.items():
        if suffix.startswith('_') and var_name.endswith(suffix):
            return type_name
    
    # 检查前缀
    for prefix, type_name in rules.items():
        if not prefix.startswith('_') and var_name.startswith(prefix):
            return type_name
    
    # 无法推断
    return 'unknown'
```

**为什么使用启发式**：
- 完全准确的类型推断需要运行代码，我们做不到
- 但通过命名规范，可以得到相当准确的猜测
- 即使猜错了，也会用 `None` 和 TODO 注释提醒用户

### 8.4 核心数据结构

**模块对象**：
```python
class Module:
    def __init__(self):
        self.id = ""              # 模块标识
        self.code = ""            # 代码内容
        self.description = ""     # 模块描述
        self.required = set()     # 需要的变量
        self.produced = set()     # 产生的变量
        self.dependencies = []    # 依赖的模块
```

**变量注册表**：
```python
class VariableRegistry:
    def __init__(self):
        self.variables = {}  # 变量名 → 变量信息
    
    def register(self, name, defined_in, var_type):
        """注册一个变量"""
        self.variables[name] = {
            'name': name,
            'type': var_type,
            'defined_in': defined_in,
            'used_by': []
        }
    
    def mark_used(self, name, used_in):
        """标记变量被使用"""
        if name in self.variables:
            self.variables[name]['used_by'].append(used_in)
```

---

## 第九章：完整流程回顾

让我们用一张流程图回顾整个过程：

```
┌─────────────────────────────────────────────────────────────┐
│ 📝 任务输入                                                  │
│ "读取CSV文件，清洗数据，计算统计，保存结果"                  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 🧠 第一阶段：任务理解                                        │
│ ├─ 提取关键动作：读取、清洗、计算、保存                      │
│ ├─ 识别数据流：CSV → df → df_cleaned → statistics           │
│ └─ 建立顺序：1→2→3→4                                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 📋 第二阶段：模块生成                                        │
│ ├─ 模块1：读取CSV                                           │
│ ├─ 模块2：清洗数据                                           │
│ ├─ 模块3：计算统计                                           │
│ └─ 模块4：保存结果                                           │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 🔍 第三阶段：依赖分析（使用AST）                             │
│ ├─ 模块1需要：input_file → 产生：df                         │
│ ├─ 模块2需要：df → 产生：df_cleaned                         │
│ ├─ 模块3需要：df_cleaned → 产生：statistics                 │
│ └─ 模块4需要：statistics, output_file                       │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 🔧 第四阶段：依赖解析                                        │
│ ├─ 发现未定义：input_file, output_file                      │
│ ├─ 类型推断：都推断为字符串类型                              │
│ └─ 生成初始化代码                                            │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 📊 第五阶段：拓扑排序（使用Kahn算法）                        │
│ └─ 确定执行顺序：初始化 → 读取 → 清洗 → 统计 → 保存         │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 🎨 第六阶段：代码组装                                        │
│ ├─ 添加导入语句                                              │
│ ├─ 添加注释和文档                                            │
│ ├─ 格式化代码                                                │
│ └─ 添加错误处理                                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ ✅ 第七阶段：验证检查                                        │
│ ├─ 语法检查                                                  │
│ ├─ 变量检查                                                  │
│ └─ 逻辑检查                                                  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 🎉 完整可执行的代码！                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 第十章：实战演练 - 亲自体验

现在轮到你了！你可以运行我准备的示例程序来亲身体验整个过程：

```bash
python agent_code_planning_demo.py
```

程序会展示4个示例：

### 示例1：基础数据处理
展示如何从任务描述生成完整代码

### 示例2：依赖解析
展示如何自动检测和解决变量依赖

### 示例3：机器学习流程
展示在复杂场景下的应用

### 示例4：类型推断
展示如何根据变量名推断类型

---

## 总结：从故事到技术

通过这个故事，你了解了：

### 设计思路（为什么）
- ✨ 为什么需要任务分解？ → 化繁为简
- ✨ 为什么需要依赖分析？ → 避免错误
- ✨ 为什么需要拓扑排序？ → 确定顺序
- ✨ 为什么需要类型推断？ → 自动修复

### 实现方法（怎么做）
- 🔧 使用AST分析代码结构
- 🔧 使用Kahn算法排序
- 🔧 使用启发式规则推断类型
- 🔧 使用注册表跟踪变量

### 实际效果（是什么）
- ✅ 自动生成完整、可执行的代码
- ✅ 避免"变量未定义"错误
- ✅ 保证模块间衔接自然
- ✅ 提供清晰的代码注释

---

## 下一步

如果你想深入了解：

1. **技术细节** → 阅读《智能体代码规划与协同实现详解.md》
2. **代码实现** → 阅读 `agent_code_planning_demo.py` 源代码
3. **可视化流程** → 阅读《流程图与架构.md》
4. **快速参考** → 阅读《代码规划协同实现说明.md》

希望这个故事能帮助你理解智能体代码规划的全过程！如果还有疑问，随时可以问我。

---

*这是一个AI智能体的自述，讲述了它如何从理解任务到生成代码的完整旅程。*
