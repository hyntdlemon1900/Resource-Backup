# 代码规划与协同实现说明

## 📁 文件说明

本目录包含以下与智能体代码规划和协同实现相关的文件：

### 1. 智能体代码规划与协同实现详解.md
**详细的技术文档**，包含：
- 代码规划机制的完整理论说明
- 变量依赖管理的实现细节
- 代码协同的核心算法
- 拓扑排序、AST分析等关键技术
- 多个实现示例和代码片段

适合：需要深入理解智能体系统如何进行代码规划的读者

### 2. agent_code_planning_demo.py
**可直接运行的完整示例程序**，包含：
- 变量注册表（VariableRegistry）
- 依赖解析器（DependencyResolver）
- 代码规划器（CodePlanner）
- 4个演示示例

适合：希望通过实际运行的代码来理解系统工作原理的读者

## 🚀 快速开始

### 运行演示程序

```bash
# 进入项目目录
cd /path/to/Resource-Backup

# 运行演示程序
python agent_code_planning_demo.py
```

### 程序输出

程序会依次运行4个示例：

1. **示例1：基础数据处理流程**
   - 演示从任务描述到代码生成的完整过程
   - 展示如何处理数据加载、清洗、统计、保存等操作

2. **示例2：依赖解析演示**
   - 展示如何检测变量依赖关系
   - 演示自动生成变量初始化代码
   - 展示拓扑排序确保正确的执行顺序

3. **示例3：机器学习流程**
   - 演示在复杂场景下的代码规划
   - 包含数据加载、分割、训练、评估等步骤

4. **示例4：变量类型推断**
   - 展示如何根据变量名推断类型
   - 演示不同类型变量的初始化方式

## 🔑 核心概念

### 1. 代码规划 (Code Planning)

智能体系统将复杂任务分解为可执行的代码模块：

```
任务描述 → 任务分析 → 模块生成 → 依赖分析 → 代码生成
```

**关键技术：**
- 任务分解算法
- 模块识别
- 执行顺序规划

### 2. 变量依赖分析 (Dependency Analysis)

使用AST（抽象语法树）分析代码中的变量：

```python
# 分析这段代码
result = x * 2
y = result + 10

# 得到依赖关系
# result 需要 x
# y 需要 result
```

**关键技术：**
- AST解析
- 变量追踪
- 作用域管理

### 3. 依赖解析 (Dependency Resolution)

自动解决变量依赖问题：

```python
# 原始模块
Module 1: result = x * 2
Module 2: y = result + 10

# 检测到x未定义
# 自动添加初始化模块
Module 0: x = None  # 自动生成
Module 1: result = x * 2
Module 2: y = result + 10
```

**关键技术：**
- 依赖检测
- 类型推断
- 自动初始化

### 4. 拓扑排序 (Topological Sort)

确保模块按正确顺序执行：

```
Module A (产生x) → Module B (使用x, 产生y) → Module C (使用y)
```

**关键技术：**
- Kahn算法
- 依赖图构建
- 循环检测

## 📚 代码结构

### VariableRegistry (变量注册表)

```python
registry = VariableRegistry()

# 分析代码的变量依赖
deps = registry.analyze_dependencies(code)
# 返回: {'required': [...], 'produced': [...]}
```

**功能：**
- 使用AST分析代码
- 识别变量定义（Store）和使用（Load）
- 过滤内置函数

### DependencyResolver (依赖解析器)

```python
resolver = DependencyResolver(registry)

# 解决模块间的依赖
resolved = resolver.resolve_dependencies(modules)
# 返回: 添加了初始化模块并排序后的模块列表
```

**功能：**
- 检测未定义变量
- 推断变量类型
- 生成初始化代码
- 拓扑排序

### CodePlanner (代码规划器)

```python
planner = CodePlanner()

# 从任务描述生成代码
code = planner.plan_and_generate(task_description)
# 返回: 完整的可执行代码
```

**功能：**
- 任务分析
- 模块生成
- 依赖解析
- 代码组装

## 🔬 技术细节

### AST分析示例

```python
import ast

code = "result = x * 2"
tree = ast.parse(code)

# 遍历AST节点
for node in ast.walk(tree):
    if isinstance(node, ast.Name):
        if isinstance(node.ctx, ast.Store):
            print(f"定义变量: {node.id}")
        elif isinstance(node.ctx, ast.Load):
            print(f"使用变量: {node.id}")
```

### 拓扑排序示例

```python
def topological_sort(graph):
    """Kahn算法实现"""
    # 计算入度
    in_degree = {node: 0 for node in graph}
    for node in graph:
        for neighbor in graph[node]:
            in_degree[neighbor] += 1
    
    # 找出入度为0的节点
    queue = [n for n in in_degree if in_degree[n] == 0]
    result = []
    
    while queue:
        node = queue.pop(0)
        result.append(node)
        
        for neighbor in graph[node]:
            in_degree[neighbor] -= 1
            if in_degree[neighbor] == 0:
                queue.append(neighbor)
    
    return result
```

### 类型推断示例

```python
def infer_type(var_name):
    """根据变量名推断类型"""
    if var_name.endswith('_list'):
        return 'list'
    elif var_name.endswith('_dict'):
        return 'dict'
    elif var_name.startswith('is_') or var_name.startswith('has_'):
        return 'bool'
    elif var_name.startswith('num_') or var_name.startswith('count_'):
        return 'int'
    else:
        return 'unknown'
```

## 🎯 应用场景

### 1. 自动代码生成
- 从自然语言描述生成代码
- 自动处理变量依赖
- 确保代码可执行

### 2. 代码重构
- 分析现有代码的依赖关系
- 自动重新排序代码块
- 检测潜在的依赖问题

### 3. 教学辅助
- 展示代码执行顺序
- 可视化变量依赖关系
- 帮助理解程序结构

### 4. 代码审查
- 自动检测未定义变量
- 分析变量作用域
- 发现潜在的逻辑错误

## 📖 学习路径

### 初学者

1. **阅读**：先阅读 `智能体代码规划与协同实现详解.md` 的概述部分
2. **运行**：执行 `agent_code_planning_demo.py` 查看实际效果
3. **理解**：观察程序输出，理解各个步骤的含义
4. **修改**：尝试修改示例中的任务描述，观察生成的代码变化

### 进阶学习

1. **深入阅读**：详细阅读技术文档中的实现细节
2. **代码分析**：阅读 `agent_code_planning_demo.py` 的源代码
3. **自定义**：添加新的任务类型和模块模板
4. **扩展**：实现更复杂的类型推断规则

### 高级应用

1. **集成**：将这些技术集成到自己的项目中
2. **优化**：优化算法性能，处理更大规模的代码
3. **扩展**：支持更多编程语言
4. **研究**：探索更先进的代码分析技术

## 🛠️ 依赖要求

### 基本要求
- Python 3.6+
- 标准库：ast, re, typing, collections

### 可选依赖（用于完整功能）
```bash
pip install pandas numpy scikit-learn matplotlib
```

这些库在演示程序的示例代码中被使用，但不影响核心功能的运行。

## 💡 常见问题

### Q1: 为什么有些变量被初始化为 None？
**A:** 当系统无法推断变量的具体类型时，会使用 `None` 作为默认值，并添加 `TODO` 注释提醒用户手动指定。

### Q2: 拓扑排序失败怎么办？
**A:** 这通常意味着存在循环依赖。检查模块间的依赖关系，确保没有模块相互依赖。

### Q3: 如何添加新的变量类型推断规则？
**A:** 修改 `DependencyResolver._infer_type()` 方法，添加新的命名规则匹配。

### Q4: 能处理多文件项目吗？
**A:** 当前实现主要针对单文件场景。对于多文件项目，需要扩展以支持跨文件的依赖分析。

### Q5: 如何处理第三方库的导入？
**A:** AST分析会自动识别 import 语句，并将导入的名称添加到"产生的变量"列表中。

## 🔗 相关资源

### 技术文档
- Python AST 文档: https://docs.python.org/3/library/ast.html
- 图算法教程: https://www.geeksforgeeks.org/topological-sorting/

### 论文和资料
- 静态代码分析技术
- 程序依赖图 (Program Dependence Graph)
- 数据流分析 (Data Flow Analysis)

## 📝 总结

本项目实现了一个完整的智能体代码规划与协同系统，核心特点：

✅ **自动化**：自动分析任务、生成模块、解决依赖
✅ **准确性**：使用AST确保准确的代码分析
✅ **可靠性**：通过拓扑排序确保正确的执行顺序
✅ **智能性**：自动推断类型、生成初始化代码
✅ **可扩展**：模块化设计，易于扩展和定制

通过学习和使用这些代码，你可以：
- 理解智能体系统的代码规划机制
- 掌握变量依赖分析技术
- 学习图算法在实际项目中的应用
- 为自己的项目实现类似功能

## 📧 反馈与贡献

如有问题或建议，欢迎提出Issue或Pull Request。

---

最后更新：2025年11月
